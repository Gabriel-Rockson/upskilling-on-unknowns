const numbers = [2, 3, 4, 5]

// unshift will add the element to the start of the array unlike push
// unshift will also return the new length of the array after the addition


console.log("Before ", numbers)

numbers.unshift(0, 1)

console.log("After", numbers)
