// shift method is like the pop method, but this method removes elements from the 
// start of the array.
// shift method is to pop
// unshift method is to push

const names = ["Gabriel", "Melinda"]


console.log({ names })

const removedName = names.shift()

console.log({ removedName })
console.log({ names })

